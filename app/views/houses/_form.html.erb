
  <%= form_for(@house) do |f| %>
    <% if @house.errors.any? %>
      <div id="error_explanation">
        <h2><%= pluralize(@house.errors.count, "error") %> prohibited this house from being saved:</h2>

        <ul>
        <% @house.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
        </ul>
      </div>
    <% end %>
<div class="container">
  <div class="row">
    <!-- <div class="col s6">
      <img src="bg_castle"></img>
    </div> -->

  <form class="col s6">
      <div class="input-field col s12">
        <%= f.label :name %><br>
        <%= f.text_field :name %>
      </div>
      <div class="input-field col s12">
        <%= f.label :location %><br>
        <%= f.text_field :location %>
      </div>
      <div class="input-field col s12">
        <%= f.label :street %><br>
        <%= f.text_field :street %>
      </div>
      <div class="input-field col s12">
        <%= f.label :size %><br>
        <%= f.text_field :size %>
      </div>
      <button class="btn waves-effect waves-light" type="submit" name="action">Create House
        <i class="mdi-content-send right"></i>
        <div class="actions">
          <%= f.submit %>
        </div>
      </button>
      <a class="waves-effect waves-light btn" <%= link_to 'Back', houses_path %>
      </a>
  </form>
  </div>
</div>
<% end %>
